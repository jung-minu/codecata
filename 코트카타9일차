class Solution {
    fun solution(array: IntArray, height: Int): Int {
        
        var answer: Int = 0
        
       for(i in array){
           
           if(i > height)
           
           answer++
       }
        
        
        return answer
    }
}

// 증감연산자의 활용법, 반복문/조건문 복습
